
# =======================
# DATABASE CONFIGURATION
# =======================

# PostgreSQL database credentials
DB_USERNAME=postgres       # Database username (default is 'postgres')
DB_PASSWORD=postgres      # Database password (change this in production!)
DB_HOST=postgres          # Database host (service name in Docker)
DB_PORT=5432              # Default PostgreSQL port
DB_NAME=wild_oasis        # Your database name

# Connection pool settings (optimizes database connections)
DB_IDLE_CONNECTION=10     # Maximum idle connections to maintain
DB_MAX_CONNECTION=100    # Maximum total connections allowed (typo fixed: "CONNECTION")
DB_MAX_LIFE_TIME=3600    # Maximum connection lifetime in seconds (1 hour)

# =======================
# CLOUDINARY CONFIGURATION  
# =======================

# Cloudinary credentials for image storage
# Get these from your Cloudinary dashboard: https://cloudinary.com/console
CLOUDINARY_API_SECRET=   # Your Cloudinary API secret (keep secure!)
CLOUDINARY_API_KEY=      # Your Cloudinary API key  
CLOUDINARY_CLOUD_NAME=   # Your Cloudinary cloud name (e.g., "your-cloud-name")

# =======================
# AUTHENTICATION CONFIGURATION
# =======================

# JSON Web Token (JWT) secret
# Generate a strong secret for production (e.g., 64-character random string)
# NEVER commit real secrets to version control!
JWT_SECRET=             # Secret key for signing authentication tokens